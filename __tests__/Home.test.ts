import { render } from '@testing-library/react';
import Home from '@/app/page';

describe('home', () => {
  it('should render', () => {
    // render(<Home />)
  });
});
// render 안에서 선언하는 컴포넌트가 이상하다 슈  발...
